# this function gets the arduinoID corresponding to a given location

def getArduinoID(location):
    # index into some sort of table or database to get the arduinoID
    # arduinoID = table(location)
    return arduinoID
