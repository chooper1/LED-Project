# this function sends the location and RFID to the microcontroller

def sendPartLocationAndRFID(location, RFID):
    #use built in library to transmit location to the arduino
    #use built in library to transmit RFID to the arduino
    #the arduino should block while waiting for these two commands
    
